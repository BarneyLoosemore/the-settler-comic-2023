<%- include('_header', { preloads: null, prefetches: null, stylesheet }) %>
<main>
  <ul>
    <% for (const issue of issues) { %> <% for (const page of issue.pages) { %>
    <li>
      <a href="/issue-<%= issue.number %>#<%= page.title %>">
        <%= page.title %>
      </a>
    </li>
    <% } %> <% } %>
  </ul>
</main>
<%- include('_footer') %>
